# Optional — only needed if the backend has STRIPE_SECRET_KEY set (TEST MODE).
# Stripe -> Dashboard -> Developers -> API keys -> Publishable key. Safe to expose
# client-side by design (Stripe's own model), unlike the secret key.
VITE_STRIPE_PUBLISHABLE_KEY=
