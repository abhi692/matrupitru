// The phone and the dev machine must be on the same Wi-Fi network. Update this
// if your PC's LAN IP changes (check with `ipconfig` on Windows, look for the
// Wi-Fi adapter's IPv4 address). Backend must be running on this machine at
// this port (npm run dev inside backend/).
export const API_BASE_URL = 'http://192.168.18.10:4000/v1';
